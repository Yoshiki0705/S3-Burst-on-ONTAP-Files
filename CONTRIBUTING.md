# CONTRIBUTING

Issue と Pull Request を歓迎します。この文書は執筆・実装の規約と、マージ前に通すゲートを
まとめたものです。AI エージェント向けの指示は [AGENTS.md](AGENTS.md) にあります。

## 環境の用意

```bash
pip install -r requirements-dev.txt   # ruff / pytest / cfn-lint / zizmor（バージョン固定）
npm install -g markdownlint-cli2      # pip では入らない
brew install gitleaks                 # pip では入らない
```

バージョンを固定しているのは、lint の判定が実行した日に依存しないようにするためです。
`make python` は導入済みの `ruff` が `requirements-dev.txt` と違うときに警告します。
その警告が出ている間、ローカルで通っても CI で通るとは限りません。固定版を入れてください。

## ゲート

```bash
make help    # ターゲット一覧
make all     # コミットゲート
```

`make all` が通ることを、**最後の編集のあとに**確認してください。
緑になったあとで 1 ファイル触ると（CHANGELOG の追記が典型です）、その分は検査されていません。

**ゲートの結果は、次の操作を止める形で繋いでください。**

```bash
make ready MSG="docs: ..." && git commit -F <message-file>
```

`;` で並べると失敗が伝わりません。**`make all 2>&1 | tail -2 && git commit` も同じく伝わりません** —
パイプラインの終了コードは最後のコマンド（`tail`）のもので、常に 0 です。
`set -o pipefail` の無いシェルでは特にそうです。**読んでから従うかどうかの問題ではなく、
出力を読む前にコミットが済んでいる**形になります。

| ターゲット | 何を見るか |
|---|---|
| `make lint` | Markdown / Python / CloudFormation |
| `make i18n-check` | 日本語版と英語版の節構成が一致しているか |
| `make drift` | 表の中身（条件キー・API 名・バージョン・数値）が言語間でずれていないか。節構成の一致では捕まらない差分です |
| `make ready MSG="..."` | **コミット前の 1 コマンド。** `make all` + 件名検査。`&&` でコミットに繋ぎます |
| `make pr-verify PR=<n>` | PR が現在指しているコミットに対して CI が通ったか。SHA で照合します（ネットワークと `gh` が必要） |
| `make external-anchors` | 引用している姉妹リポジトリの節アンカーが生きているか（ローカルチェックアウトが無ければスキップ） |
| `make switcher-check` | 言語スイッチャーが実在する言語と一致しているか、他言語へ誘導していないか |
| `make audit` | 命名・出典マーカー・比較の書き方・個人情報・機構の混同・誇張語 |
| `make secrets` | gitleaks |
| `make iac-security` | テンプレートとワークフローのセキュリティ姿勢（checkov）。抑制には `# checkov:skip=<id>: 理由` で理由を必ず書きます |
| `make zizmor` | ワークフローファイル自体の CI セキュリティ監査（pedantic） |
| `make links` | 内部リンクの解決（`make links-external` で外部 URL も） |
| `make budget` | AGENTS.md のサイズと `.kiro/` ローダーの到達性 |
| `make en-lang` | 英語ドキュメントに日本語が残っていないか |
| `make counts` | 本文に書かれた数がファイルシステムと一致しているか |
| `make pattern-status` | 各パターンの README が、定義された 5 つの状態のどれかで始まっているか |
| `make test` | 検出したテストディレクトリを 1 プロセスずつ |

`make links` は内部リンクだけを見ます。外部 URL は毎週 `.github/workflows/link-rot.yml` が
`make links-external` と同じ検査を回し、届かない URL があれば Issue に報告します。
**プルリクエストでは落としません。** 外部サイトの都合で赤くなる検査は、無関係な変更のときに
赤を無視する習慣を作るためです。手元で確かめるなら `make links-external`（ネットワークが必要）。

## ドキュメントの書き方

- 日本語が原文です。英語は Tier 1（ハブと `docs/i18n-manifest.txt` に列挙したもの）だけ訳します。
- 文書の言語はディレクトリで表します（`docs/ja/` / `docs/en/`）。ファイル名に `.en` は付けません。
  例外はルート `README.md` で、これが日本語のハブです。したがって `docs/ja/README.md` は
  存在しません。
- 翻訳は「コピーして本文を置き換える」だけで済むように、同じ深さに置きます。
  翻訳中に `../` の数を調整することになったら、置き場所が間違っています。
- **言語スイッチャーを手で書かないでください。** `make switcher-write` が生成します。
  新しいファイルにはマーカー対（H1 の直後と末尾）を 1 度だけ足します。
- 図は Mermaid、構成図は draw.io。**図に書いたことは必ず本文か表にも書いてください。**
  Mermaid は環境によって描画されず、スクリーンリーダーからも読み取りにくく、クローラーも
  抽出できません。図の中にしか存在しない判断は、一部の読者に届きません。
- 2 つ以上の属性を持つ項目は箇条書きより表にします。
- 節が 7 つを超えたら `<details>` に畳むか、文書を分けます。

### 数値を書くとき

実測していない性能値・コスト値は書きません。実測した値には次を併記します。

計測日 / リージョン / ONTAP バージョン / ファイルシステムの世代と構成とスループット設定 /
オブジェクトサイズと並列度 / 何を測ったか（クライアント側かサービスのメトリクスか）。

これらを欠いた数値は再現できないので、比較にも見積りにも使えません。

### 段階を書くとき

[検証状況](docs/ja/verification-status.md)の 4 段階（検証済み / ドキュメント記載 / 未検証 /
未確認）を使います。「ドキュメントに記載がある」を「動く」として引用しないでください。
未確認は「できない」ではありません。段階を下げるのは自由で、上げるときだけ根拠が必要です。

### 比較を書くとき

各方式の**適用外条件を同じ粒度で**書きます。この構成自身の適用外条件も同じ列に書きます。
そのうえで「選び方」を必ず添えます。方式の優劣を述べる語は `make audit` が拒否します。
中立であることを宣言する語も拒否します。示すのは書き方で行います。

### 見出しを書くとき

`##` 以下の節見出しは**体言止め（名詞句）**にします。動詞終止形・疑問形・述語文は、読者が
ラベルを期待する位置に文が来るため読みにくくなります。

| 型 | 避ける | 使う |
|---|---|---|
| 動詞終止形 | パターンを追加する | パターンの追加 |
| 疑問形 | なぜツールが 2 つなのか | ツールが 2 つである理由 |
| 述語文 | リネームは両方の層で高くつく | 両方の層で高くつくリネーム |

**名詞化で断定を落とさないでください。** 見出しが知見そのものを運んでいることが多く、
「削除が無言で戻る」を「削除の挙動」にすると断定が語に退化します。接尾語か修飾語で断定を保ちます。

- 接尾語: 〜の存在 / 〜の不在 / 〜の成立 / 〜の不成立 / 〜の必要 / 〜の不可 / 〜の差 / 〜の上限 /
  〜の実行 / 〜の確認 / 〜の削除 / 〜の限定 / 〜の可否
- 修飾: 未対応の〜 / 既定で無効な〜 / 無言で戻る〜 / 階層化できない〜

**どの接尾語でも断定が保てない見出しは、文を 1 つ抱えています。本文へ移してください。**

対象外は 4 つです。**H1 と frontmatter の `title`**（別規約で「1 行の主張文」と定めているため、
同じ文字列がタイトルとしては正しく節見出しとしては誤りになります）、**英語の見出し**、
**コードブロック内の `#` で始まる行**（シェルのコメントです。検出器は必ずフェンスを除外してください)、
表のセルと箇条書きの行。

**見出しを改名するとアンカーが変わります。** `grep -rn '#<変換前のアンカー>' .` で参照元を洗い、
同じコミットで直します。`make links` は内部リンクを捕まえますが、**GitHub は未知のフラグメントを
ページ先頭で返すので、外部から引用している側は壊れたことに気づけません。** 他リポジトリから
引用されている見出しは、改名前に相手へ知らせます（この向きの逆は `make external-anchors` が見ます）。

検出は次の形で行います。フェンスの除外と H1 の除外が要点です。

```python
BAD  = r'(ます|ません|です|でした|ください|か|[うくぐすずつぬふぶむるれ])$'   # 文で終わる
NOUN = r'(問い|項目|手順|範囲|一覧|理由|条件|場合|点|扱い|こと|もの|形|型)$'  # 名詞で終わる
HEAD = r'^(#{2,6})\s+(.*?)\s*$'                                             # H1 は含めない
```

## 検査が捕まえないドリフト

**値のドリフトは機械検出できます。根拠のドリフトは検出できません。** この区別は運用に影響するので
明示します。

| ドリフトの種類 | 例 | 検出 |
|---|---|---|
| 値 | 英語版の測定値だけが古い（`24,861` → `24,681`）、条件キーの綴り違い、バージョン違い | `make drift`。表のセル限定・数値は値で比較・片方向 |
| **根拠** | 同じ主張の裏づけを「対応表の記載」から「1 回の観測」へ変え、**片方の言語だけ**直す | **できません** |

根拠のドリフトを機械検出する案は 1 つ実装して落としました。段階の語
（`実測`↔`measured` 等）を [i18n-terms.md](docs/i18n-terms.md) の対応表から読んで比較する形で、
**クリーンなツリーで 7 件報告し 7 件すべて誤検知**でした。英語の語形変化（measure /
measured / measurement）に部分一致が耐えず、語形変化を許すと今度は **`verified` と
`not verified` を区別できなくなります。** 主張の強さを守る検査が主張を反転させる誤検知を出すのは
受け入れられないので、規則ごと削除しました。経緯は
[`tools/check_translation_drift.py`](tools/check_translation_drift.py) の docstring と
[`tests/test_translation_drift.py`](tests/test_translation_drift.py) にあります。

**したがって規律で担保します。主張の根拠を変えるときは、日本語版と英語版を同じコミットで触ってください。**
`make i18n-check` は節構成しか見ず、`make drift` はリテラルしか見ません。**どちらも通ります。**
この形の欠陥は 1 度実際に公開しています（`UploadPartCopy` の行）。

## パターンの追加

```bash
make new-pattern AXIS=collect SLUG=s3ap-ingest
```

`AXIS` は `collect` / `serve` / `pipelines` のいずれかです。生成された雛形を編集します。

| ファイル | 役割 |
|---|---|
| `template.yaml` | 単体でデプロイできること。`cfn-lint` がエラーゼロ |
| `README.md` | 前提・デプロイ手順・確認方法・削除手順。削除手順は順序まで書く |
| `params.example.json` | 実際の値は入れない |
| `tests/` | pytest。`make test` が自動で見つけます |
| `docs/` | 必要な場合のみ |

パターン数は本文に書かないでください。`make counts` が
`patterns/*/*/template.yaml` から数え、食い違えば失敗します。
どうしても数を書く場合も、同じ検査の対象になります。

## 他リポジトリからの資産の持ち込み

**先頭に出典と、元と分岐したことを書いてください。**「同じ内容が 2 か所」は、
片方が古くなる代償を承知の上で選んだ状態です。黙って複製しないでください。

記録する内容は、出典の URL、持ってきた範囲、変えた点、変えた理由です。
既存の例は [docs/ja/support-matrix.md](docs/ja/support-matrix.md) と
[docs/ja/poc-checklist.md](docs/ja/poc-checklist.md) の先頭にあります。

姉妹リポジトリ `FSx-for-ONTAP-S3AccessPoints-Serverless-Patterns` の `solutions/flexcache/` は**移動しません**。
あちらの README とテストとテスト対象一覧がそのディレクトリを数えているため、移動すると
あちらの `make drift` と `make test` が壊れます。

## 公開物の安全

このリポジトリは公開されています。git の履歴は永続し、検索に載ります。

個人名・メールアドレス・電話番号・住所・アカウント ID・内部 IP・ホスト名・
サポートケース番号・ベンダー内部のチケット ID・顧客名・マスクしていないスクリーンショットは
コミットしません。役割ベースの表記とプレースホルダーを使います。詳細は
[AGENTS.md](AGENTS.md) の該当節にあります。

役職名やペルソナ名を付けた inline note（`> **… lens**:`、`> **… の視点**:`）は書きません。
実際にその役割の人がレビューしたという含みが出ます。中立なトピック名
（`> **セキュリティに関する補足**:`）を使ってください。

レビュー過程のメタデータ（ラウンド数、レビュー日、レンズ数）も公開物には残しません。

## 不可逆操作

削除できなくする機能（SnapLock、改ざん防止 Snapshot、Object Lock、Vault Lock など）は、
**保持期間を明示した指示がない限り有効化しません。** 検証環境も例外ではありません。
削除できない検証リソースは長期の請求になり、同居する他のリソースも動かせなくします。
背景と手順は [AGENTS.md](AGENTS.md) にあります。

## Issue と Pull Request

Issue のタイトル: `<動詞> <対象>`。再現手順、期待、実際を書いてください。
ドキュメントの誤りは、どのファイルのどの記述かを示してください。

Pull Request のタイトルは Conventional Commits で 70 文字以内です。
`.github/workflows/pr-title-check.yml` が検査します。

```text
feat:     パターンや機構の追加
fix:      不具合の修正
docs:     ドキュメントのみ
bench:    実測値の追加・更新
refactor: 挙動を変えない整理
test:     テストのみ
ci:       ワークフローとゲート
chore:    その他
```

ブランチ名は `<type>/<対象>`、kebab-case、40 文字以内。
**変更で何を足すかを書き、以前が何だったかは書きません。** ブランチ名は Pull Request の
ページに残り続け、過去の作業への評価として読まれます。日付・チケット ID・個人名・
セッション番号は入れません。

コミットの subject は 72 文字以内、命令形、末尾に句点なし。
活動ではなく結果を書きます（`docs: update architecture.md` ではなく
`docs: separate the two S3-over-files mechanisms`）。
本文は「なぜ」から始めます。差分は「何を」を既に示しています。

### マージ前

1. CI が通っている。**どのコミットに対して通ったかを SHA で照合する** —
   `make pr-verify PR=<番号>`。`gh pr checks` は「最新の結果」を返すので、**読んだあとに push すると
   前のコミットの成功をもう一度読むことになります。** `make all` を最後の編集の後に走らせるのと
   同じ規則を、CI の結果にも適用します
2. 未解決のレビューコメントがない
3. 挙動が変わったならドキュメントを更新している
4. [AGENTS.md](AGENTS.md) の 4 軸セルフレビューを済ませている

## ライセンス

貢献物は [MIT License](LICENSE) の下で公開されます。
