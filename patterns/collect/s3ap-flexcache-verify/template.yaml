AWSTemplateFormatVersion: "2010-09-09"
Description: >-
  S3 Burst on ONTAP Files — Verification Environment (Existing FS Pattern).
  Creates VPC peering, security group rules, a test volume, and an EC2 host for
  NFS/SMB/S3 AP visibility measurement. Requires two existing FSx for ONTAP file
  systems (origin + cache) in separate VPCs within the same account and region.

  Post-deploy steps (ONTAP CLI + aws fsx CLI) are required for FlexCache and S3 AP
  because CloudFormation does not natively support these resources.

Metadata:
  AWS::CloudFormation::Interface:
    ParameterGroups:
      - Label:
          default: Origin File System
        Parameters:
          - OriginFileSystemId
          - OriginVpcId
          - OriginVpcCidr
          - OriginSubnetId
          - OriginRouteTableId
          - OriginSecurityGroupId
          - OriginSvmId
          - OriginSvmName
      - Label:
          default: Cache File System
        Parameters:
          - CacheFileSystemId
          - CacheVpcId
          - CacheVpcCidr
          - CacheSubnetId
          - CacheRouteTableId
          - CacheSecurityGroupId
          - CacheSvmName
      - Label:
          default: Test Host
        Parameters:
          - InstanceType
          - LatestAmiId
      - Label:
          default: Volume Configuration
        Parameters:
          - OriginVolumeName
          - OriginVolumeSizeMB
          - SecurityStyle
      - Label:
          default: Least Privilege (narrow after the post-deploy steps)
        Parameters:
          - S3AccessPointArn
          - SmbCredentialSecretName

Parameters:
  # --- Origin ---
  OriginFileSystemId:
    Type: String
    AllowedPattern: "^fs-[0-9a-f]{17}$"
  OriginVpcId:
    Type: AWS::EC2::VPC::Id
  OriginVpcCidr:
    Type: String
    # A private range only, and no prefix shorter than /8. This value becomes the source CIDR of
    # every security group rule below, so `0.0.0.0/0` here silently opens NFS, SMB and the ONTAP
    # intercluster ports to the internet -- and the stack would create successfully.
    AllowedPattern: "^(10|172|192)\\.[0-9]{1,3}\\.[0-9]{1,3}\\.[0-9]{1,3}/(8|9|1[0-9]|2[0-9]|3[0-2])$"
    ConstraintDescription: >-
      Must be a private IPv4 CIDR (10/8, 172.16/12 or 192.168/16) with a prefix of /8 or longer.
      This value is the source of every security group rule this stack creates.
    Description: "CIDR block of the origin VPC (e.g. 192.168.0.0/16)"  # allow:pii
  OriginSubnetId:
    Type: AWS::EC2::Subnet::Id
  OriginRouteTableId:
    Type: String
    Description: Route table ID for the origin subnet
  OriginSecurityGroupId:
    Type: AWS::EC2::SecurityGroup::Id
  OriginSvmId:
    Type: String
    Description: "SVM ID on the origin (e.g. svm-0123456789abcdef0)"
  OriginSvmName:
    Type: String
    Default: svm1
    Description: SVM name on the origin cluster

  # --- Cache ---
  CacheFileSystemId:
    Type: String
    AllowedPattern: "^fs-[0-9a-f]{17}$"
  CacheVpcId:
    Type: AWS::EC2::VPC::Id
  CacheVpcCidr:
    Type: String
    # A private range only, and no prefix shorter than /8. This value becomes the source CIDR of
    # every security group rule below, so `0.0.0.0/0` here silently opens NFS, SMB and the ONTAP
    # intercluster ports to the internet -- and the stack would create successfully.
    AllowedPattern: "^(10|172|192)\\.[0-9]{1,3}\\.[0-9]{1,3}\\.[0-9]{1,3}/(8|9|1[0-9]|2[0-9]|3[0-2])$"
    ConstraintDescription: >-
      Must be a private IPv4 CIDR (10/8, 172.16/12 or 192.168/16) with a prefix of /8 or longer.
      This value is the source of every security group rule this stack creates.
    Description: "CIDR block of the cache VPC (e.g. 10.128.0.0/16)"  # allow:pii
  CacheSubnetId:
    Type: AWS::EC2::Subnet::Id
  CacheRouteTableId:
    Type: String
    Description: Route table ID for the cache subnet
  CacheSecurityGroupId:
    Type: AWS::EC2::SecurityGroup::Id
  CacheSvmName:
    Type: String
    Default: svm1

  # --- Test host ---
  InstanceType:
    Type: String
    Default: t3.micro
    AllowedValues: [t3.micro, t3.small, t3.medium]
  LatestAmiId:
    Type: AWS::SSM::Parameter::Value<AWS::EC2::Image::Id>
    Default: /aws/service/ami-amazon-linux-latest/al2023-ami-kernel-default-x86_64

  # --- Volume ---
  OriginVolumeName:
    Type: String
    Default: s3burst_verify
    AllowedPattern: "^[a-zA-Z][a-zA-Z0-9_]*$"
  OriginVolumeSizeMB:
    Type: Number
    Default: 5120
    MinValue: 1024
  SecurityStyle:
    Type: String
    Default: UNIX
    AllowedValues: [UNIX, NTFS]
    Description: "UNIX for NFS + S3 AP (recommended). NTFS for SMB-primary."

  # The access point is created after this stack, out of band (step 2 of the post-deploy steps),
  # so its ARN cannot be known at first deployment. Deploy once with the default, then update the
  # stack with the ARN that `create-and-attach-s3-access-point` returned. Leaving it at "*" grants
  # the four object actions on every bucket the role can reach, which is only tolerable in an
  # isolated verification account.
  S3AccessPointArn:
    Type: String
    Default: "*"
    Description: >-
      Access point ARN from the post-deploy step, or "*" on first deployment.
      Update the stack with the real ARN once the access point exists.

  # `secret:*` matches every secret in the account. The measurement reads exactly one.
  SmbCredentialSecretName:
    Type: String
    Default: s3burst/smb-reader
    AllowedPattern: "^[a-zA-Z0-9/_+=.@-]+$"
    Description: >-
      Name of the Secrets Manager secret holding the SMB password, passed to
      measure_visibility.py as --smb-cred-secret. Only this secret is readable.

Conditions:
  S3AccessPointUnset: !Equals [!Ref S3AccessPointArn, "*"]

Resources:
  # --- VPC Peering ---
  VpcPeering:
    Type: AWS::EC2::VPCPeeringConnection
    Properties:
      VpcId: !Ref CacheVpcId
      PeerVpcId: !Ref OriginVpcId
      Tags:
        - Key: Name
          Value: !Sub s3burst-verify-${AWS::StackName}

  CacheToOriginRoute:
    Type: AWS::EC2::Route
    DependsOn: VpcPeering
    Properties:
      RouteTableId: !Ref CacheRouteTableId
      DestinationCidrBlock: !Ref OriginVpcCidr
      VpcPeeringConnectionId: !Ref VpcPeering

  OriginToCacheRoute:
    Type: AWS::EC2::Route
    DependsOn: VpcPeering
    Properties:
      RouteTableId: !Ref OriginRouteTableId
      DestinationCidrBlock: !Ref CacheVpcCidr
      VpcPeeringConnectionId: !Ref VpcPeering

  # --- Security Group Rules ---
  # Cache SG: allow from Origin VPC
  CacheSgIntercluster:
    Type: AWS::EC2::SecurityGroupIngress
    Properties:
      GroupId: !Ref CacheSecurityGroupId
      IpProtocol: tcp
      FromPort: 10000
      ToPort: 10000
      CidrIp: !Ref OriginVpcCidr
      Description: ONTAP intercluster
  CacheSgInterclusterData:
    Type: AWS::EC2::SecurityGroupIngress
    Properties:
      GroupId: !Ref CacheSecurityGroupId
      IpProtocol: tcp
      FromPort: 11104
      ToPort: 11105
      CidrIp: !Ref OriginVpcCidr
      Description: ONTAP intercluster data
  CacheSgNfs:
    Type: AWS::EC2::SecurityGroupIngress
    Properties:
      GroupId: !Ref CacheSecurityGroupId
      IpProtocol: tcp
      FromPort: 2049
      ToPort: 2049
      CidrIp: !Ref OriginVpcCidr
      Description: NFS
  CacheSgSmb:
    Type: AWS::EC2::SecurityGroupIngress
    Properties:
      GroupId: !Ref CacheSecurityGroupId
      IpProtocol: tcp
      FromPort: 445
      ToPort: 445
      CidrIp: !Ref OriginVpcCidr
      Description: SMB

  # Origin SG: allow from Cache VPC
  OriginSgIntercluster:
    Type: AWS::EC2::SecurityGroupIngress
    Properties:
      GroupId: !Ref OriginSecurityGroupId
      IpProtocol: tcp
      FromPort: 10000
      ToPort: 10000
      CidrIp: !Ref CacheVpcCidr
      Description: ONTAP intercluster
  OriginSgInterclusterData:
    Type: AWS::EC2::SecurityGroupIngress
    Properties:
      GroupId: !Ref OriginSecurityGroupId
      IpProtocol: tcp
      FromPort: 11104
      ToPort: 11105
      CidrIp: !Ref CacheVpcCidr
      Description: ONTAP intercluster data
  OriginSgNfs:
    Type: AWS::EC2::SecurityGroupIngress
    Properties:
      GroupId: !Ref OriginSecurityGroupId
      IpProtocol: tcp
      FromPort: 2049
      ToPort: 2049
      CidrIp: !Ref CacheVpcCidr
      Description: NFS
  OriginSgSmb:
    Type: AWS::EC2::SecurityGroupIngress
    Properties:
      GroupId: !Ref OriginSecurityGroupId
      IpProtocol: tcp
      FromPort: 445
      ToPort: 445
      CidrIp: !Ref CacheVpcCidr
      Description: SMB
  OriginSgOntapApi:
    Type: AWS::EC2::SecurityGroupIngress
    Properties:
      GroupId: !Ref OriginSecurityGroupId
      IpProtocol: tcp
      FromPort: 443
      ToPort: 443
      CidrIp: !Ref CacheVpcCidr
      Description: ONTAP REST API

  # --- Test Host (in cache VPC, access via SSM) ---
  TestHostRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: ec2.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/AmazonSSMManagedInstanceCore
      Policies:
        - PolicyName: s3burst-verify
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              # ListBucket and HeadBucket are bucket-level; PutObject and GetObject are
              # object-level. Both are expressed against the access point ARN because the
              # access point is what the measurement addresses, and the object-level form
              # is that ARN followed by /object/*.
              # `s3:HeadBucket` is not an IAM action: HeadBucket is authorised by
              # `s3:ListBucket`. It was listed here and had no effect.
              - Sid: S3AccessPointBucketLevel
                Effect: Allow
                Action:
                  - s3:ListBucket
                Resource: !Ref S3AccessPointArn
              - Sid: S3AccessPointObjectLevel
                Effect: Allow
                Action:
                  - s3:PutObject
                  - s3:GetObject
                Resource: !If
                  - S3AccessPointUnset
                  - "*"
                  - !Sub "${S3AccessPointArn}/object/*"
              - Sid: SmbCredential
                Effect: Allow
                Action: secretsmanager:GetSecretValue
                Resource: !Sub "arn:${AWS::Partition}:secretsmanager:${AWS::Region}:${AWS::AccountId}:secret:${SmbCredentialSecretName}-*"
      Tags:
        - Key: Purpose
          Value: s3burst-verification

  TestHostProfile:
    Type: AWS::IAM::InstanceProfile
    Properties:
      Roles:
        - !Ref TestHostRole

  TestHost:
    Type: AWS::EC2::Instance
    Properties:
      InstanceType: !Ref InstanceType
      ImageId: !Ref LatestAmiId
      SubnetId: !Ref CacheSubnetId
      IamInstanceProfile: !Ref TestHostProfile
      SecurityGroupIds:
        - !Ref CacheSecurityGroupId
      Tags:
        - Key: Name
          Value: !Sub s3burst-verify-${AWS::StackName}
      UserData:
        Fn::Base64: |
          #!/bin/bash
          dnf install -y nfs-utils cifs-utils sshpass python3-pip
          pip3 install boto3
          mkdir -p /opt/s3burst
          echo "Setup complete" > /opt/s3burst/.ready

  # --- Origin Volume ---
  OriginVolume:
    Type: AWS::FSx::Volume
    Properties:
      Name: !Ref OriginVolumeName
      VolumeType: ONTAP
      OntapConfiguration:
        JunctionPath: !Sub /${OriginVolumeName}
        SizeInMegabytes: !Ref OriginVolumeSizeMB
        StorageVirtualMachineId: !Ref OriginSvmId
        StorageEfficiencyEnabled: true
        SecurityStyle: !Ref SecurityStyle
        TieringPolicy:
          CoolingPeriod: 31
          Name: AUTO
      Tags:
        - Key: Purpose
          Value: s3burst-verification

Outputs:
  VpcPeeringId:
    Value: !Ref VpcPeering
  TestHostInstanceId:
    Description: Connect via SSM Session Manager
    Value: !Ref TestHost
  OriginVolumeId:
    Description: Attach S3 AP to this volume
    Value: !Ref OriginVolume
  PostDeploySteps:
    Description: Steps to complete after stack creation
    Value: !Sub |
      0. Create the UNIX user on the SVM and give it the target directory (ONTAP CLI):
         vserver services name-service unix-user create -vserver ${OriginSvmName} \
           -user s3ap-writer -id 7101 -primary-gid 7100
         # then, from an NFS client: chown 7101:7100 <mount>/  (the identity below needs write
         # permission on the volume root -- with no access point policy at all, the mode bits and
         # owner alone decide whether PutObject succeeds)
      1. Create S3 Access Point (this one identity authorizes every request; not root on purpose):
         aws fsx create-and-attach-s3-access-point --name s3burst-verify-ap --type ONTAP \
           --ontap-configuration '{"VolumeId":"${OriginVolume}","FileSystemIdentity":{"Type":"UNIX","UnixUser":{"Name":"s3ap-writer"}}}'
      2. Create cluster peer (SSH to cache cluster management):
         cluster peer create -peer-addrs <origin_intercluster_ip1>,<origin_intercluster_ip2>
      3. Create SVM peer:
         vserver peer create -vserver ${CacheSvmName} -peer-vserver ${OriginSvmName} \
           -peer-cluster <origin_cluster_name> -applications flexcache
      4. Create FlexCache:
         volume flexcache create -vserver ${CacheSvmName} -volume s3burst_verify_fc \
           -origin-vserver ${OriginSvmName} -origin-volume ${OriginVolumeName} \
           -aggr-list aggr1 -size 5g -junction-path /s3burst_verify_fc
      5. Run measurement script:
         aws ssm start-session --target ${TestHost}
         python3 /opt/s3burst/measure_visibility.py \
           --s3ap-alias <S3_AP_ALIAS> \
           --nfs-lif <CACHE_DATA_LIF> \
           --fc-path /s3burst_verify_fc \
           --origin-path /${OriginVolumeName}
